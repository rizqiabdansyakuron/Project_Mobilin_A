export default function SectionLabel({ children }) {
  return (
    <h2 className="text-[10px] font-semibold uppercase tracking-wider text-gray-400 dark:text-gray-500">
      {children}
    </h2>
  );
}
